# Weather Dashboard

A terminal-based Python script that fetches real-time NOAA Weather, Open-Meteo AQI, and UV Index for Nob Hill, displaying a graphical UI and logging the data to a CSV.

![Demo](demo.gif)

## How to run

1. Install dependencies: `pip install -r requirements.txt`
2. Run the script: `python weather_dashboard.py`
